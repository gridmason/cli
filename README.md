# cli

`@gridmason/cli` — the `gridmason` binary: `gridmason widget init / dev / lint / publish`. Widget-author devkit + the publish path into a Gridmason Registry. Public OSS (AGPL-3.0). Engineering spec: `docs/SPEC.md` · Build plan: `docs/specs/cli-v0/spec.md`.
